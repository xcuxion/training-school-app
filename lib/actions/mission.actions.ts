"use server"

import { handleError } from "../utils"

export async function new_mission_deployment() {
    try {
        
    } catch (error) {
        handleError(error)
    }
}

export async function join_mission_team() {
    try {
        
    } catch (error) {
        handleError(error)
    }
}

export async function edit_mission_details() {
    try {
        
    } catch (error) {
        handleError(error)
    }
}

export async function delete_mission() {
    try {
        
    } catch (error) {
        handleError(error)
    }
}